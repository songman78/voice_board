<div class="contents row">
  <div class="content_post" >
         <span id="current-list">選択されていません</span>
   <% if user_signed_in? && current_user.id == @tweet.user_id %>
        <ul class="more_list">
           <li>
            <%= link_to '編集', edit_tweet_path(@tweet.id), method: :get %>
          </li>
          <li>
            <%= link_to '削除', tweet_path(@tweet.id), method: :delete %>
          </li>
        </ul>
      <% end %>
      <p><%= @tweet.text %></p>
     <div class="audio">
     <audio src= "<%= "#{@tweet.file}" %>" controls="">
         <a src="<%= "#{@tweet.file}" %>">ダウンロード</a>
     </audio>
     </div>
     <div class="navbar">
    <ul class="nav-lists" id="pull-down">
      <li class="list-status">
      この問題は解決しましたか？
      <li class="list" id="list-solution">はい</li>
      <li class="list" id="list-unsolved">いいえ</li>
    </ul>
  </div>
    </div >
      <div class="container">
    <% if user_signed_in? %>
      <%= form_with(model: [@tweet, @comment], local: true) do |form| %>
        <%= form.text_area :text, placeholder: "コメントする", rows: "2" %>
        <%= form.submit "SEND" %>
      <% end %>
    <% else %>
      <strong><p>※※※ コメントの投稿には新規登録/ログインが必要です ※※※</p></strong>
    <% end %>
      <div class="comments" id="comments">
      <h4>＜コメント一覧＞</h4>
      <% @comments.reverse_each do |comment| %>
        <p>
          <strong><%= link_to comment.user.name, "/users/#{comment.user_id}" %>：</strong>
          <%= comment.text %>
        </p>
      <% end %>
    </div>
  </div>
</div>