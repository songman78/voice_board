<div class="contents row">
  <div class="container_tweets">
    <h3>投稿する</h3>
      
    <%= form_with(model: @tweet, local: true) do |form| %>
      <%= render 'shared/error_messages', model: form.object %>
       <div class="wrapper">

      <section class="main-controls">
        <canvas class="visualizer" height="60px"></canvas>
        <div id="buttons">
          <button class="record">Record</button>
          <button class="stop">Stop</button>
        </div>
      </section>

    <section class="sound-clips">


      </section>
    </div>

    <div class="content_new">
<%= javascript_pack_tag 'app.js' %>
      <p>※音声ファイルを選択してください</p>
      <%= form.file_field :file , :size => 140 %>
      <%= form.text_area :text, placeholder: "ジャンル・時代・場所など", rows: "10" %>
      <%= form.submit "投稿する" %>
       <%= form.collection_select(:check_id, Check.all, :id, :name, {}, {class:"check"}) %>
       <p>※問題が解決しましたら、編集ボタンから「解決済」を選択してください</p>
       </div>
    <% end %>
  </div>
</div>